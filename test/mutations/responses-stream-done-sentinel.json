{
  "tests": [
    "test/responses-stream-done-sentinel.test.js",
    "test/responses.test.js"
  ],
  "expectBaselinePass": 44,
  "mutations": [
    {
      "name": "the original defect — no [DONE] sentinel at all, so the /v1/responses stream ends on the terminal event and a new-api relay drops it (and its usage block)",
      "file": "src/handlers/responses.js",
      "anchor": "      if (translator.finished && !realRes.writableEnded) realRes.write('data: [DONE]\\n\\n');\n      if (!realRes.writableEnded) realRes.end();",
      "replacement": "      if (!realRes.writableEnded) realRes.end();"
    },
    {
      "name": "the guard is dropped but the write kept — the sentinel now fires unconditionally, sealing a half-delivered turn a disconnected client never settled",
      "file": "src/handlers/responses.js",
      "anchor": "      if (translator.finished && !realRes.writableEnded) realRes.write('data: [DONE]\\n\\n');\n      if (!realRes.writableEnded) realRes.end();",
      "replacement": "      if (!realRes.writableEnded) realRes.write('data: [DONE]\\n\\n');\n      if (!realRes.writableEnded) realRes.end();"
    }
  ]
}
