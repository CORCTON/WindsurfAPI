{
  "tests": [
    "test/usage-unknown-not-zero.test.js",
    "test/responses.test.js"
  ],
  "expectBaselinePass": 47,
  "mutations": [
    {
      "name": "the original defect on the streaming path — usage is always emitted, so a silent upstream is reported as 0 tokens",
      "file": "src/handlers/responses.js",
      "anchor": "        ...(hasReportedUsage(this.finalUsage) ? { usage: mapUsage(this.finalUsage) } : {}),",
      "replacement": "        usage: mapUsage(this.finalUsage),"
    },
    {
      "name": "the original defect on the non-streaming path — only one call site fixed, so the two paths disagree for the identical upstream",
      "file": "src/handlers/responses.js",
      "anchor": "    ...(hasReportedUsage(chatBody.usage) ? { usage: mapUsage(chatBody.usage) } : {}),",
      "replacement": "    usage: mapUsage(chatBody.usage || {}),"
    },
    {
      "name": "hasReportedUsage stops distinguishing an explicit zero from silence, so a cached hit's real 0 report is suppressed",
      "file": "src/handlers/responses.js",
      "anchor": "    .some(k => typeof usage[k] === 'number');",
      "replacement": "    .some(k => typeof usage[k] === 'number' && usage[k] > 0);"
    },
    {
      "name": "hasReportedUsage only consults the OpenAI spellings, so an upstream reporting input_tokens/output_tokens is treated as silent",
      "file": "src/handlers/responses.js",
      "anchor": "  return ['prompt_tokens', 'input_tokens', 'completion_tokens', 'output_tokens', 'total_tokens']",
      "replacement": "  return ['prompt_tokens', 'completion_tokens']"
    },
    {
      "name": "OVER-REACH CONTROL: usage is never emitted at all, so real token counts vanish from every response",
      "file": "src/handlers/responses.js",
      "anchor": "function hasReportedUsage(usage) {\n  if (!usage || typeof usage !== 'object') return false;",
      "replacement": "function hasReportedUsage(usage) {\n  if (usage) return false;\n  if (!usage || typeof usage !== 'object') return false;"
    }
  ]
}
