{
  "tests": [
    "test/terminal-event-guard.test.js",
    "test/stream-error.test.js"
  ],
  "expectBaselinePass": 26,
  "mutations": [
    {
      "name": "Responses: the original defect — processChunk stops consulting `finished`, so response.failed is followed by output_text.delta",
      "file": "src/handlers/responses.js",
      "anchor": "    if (this.finished) return;\n    if (chunk.created) this.createdAt = chunk.created;",
      "replacement": "    if (chunk.created) this.createdAt = chunk.created;"
    },
    {
      "name": "Anthropic: the original defect — processChunk stops consulting `messageStopped`, so a second content block opens after the error",
      "file": "src/handlers/messages.js",
      "anchor": "    if (this.messageStopped) return;\n    if (chunk.error) {",
      "replacement": "    if (chunk.error) {"
    },
    {
      "name": "Gemini: the original defect — processChunk stops consulting `finished`, so array mode appends a frame after the closing bracket and the body stops parsing",
      "file": "src/handlers/gemini.js",
      "anchor": "    if (this.finished) return;\n    if (chunk.error) {",
      "replacement": "    if (chunk.error) {"
    },
    {
      "name": "Anthropic: the guard reads the WRONG flag — sawTerminalSignal is set by the error branch itself, so the guard can never fire on a later chunk",
      "file": "src/handlers/messages.js",
      "anchor": "    if (this.messageStopped) return;\n    if (chunk.error) {",
      "replacement": "    if (this.sawTerminalSignal && false) return;\n    if (chunk.error) {"
    },
    {
      "name": "SURVIVOR (documented): Gemini writeFrame drops its writableEnded check. Harmless because it is the SECOND of two gates on the same write path and the first now holds: processChunk returns on `finished`, so nothing reaches writeFrame after a terminal event, and writableEnded only differs from `finished` when the socket closed under us — which end() already makes a no-op. Measured: with this mutation applied the suite stays 26/0, i.e. no post-terminal write becomes reachable. It is NOT dead code — it is what stops a write to an aborted socket on the CLIENT-disconnect path, which no fixture here drives. If the processChunk guard is ever removed, this mutation must be re-run: it would then become the only gate.",
      "file": "src/handlers/gemini.js",
      "anchor": "  writeFrame(obj) {\n    if (this.res.writableEnded) return;",
      "replacement": "  writeFrame(obj) {",
      "expectCaught": false
    },
    {
      "name": "OVER-REACH CONTROL: the guard fires unconditionally, so ordinary content never streams at all",
      "file": "src/handlers/responses.js",
      "anchor": "    if (this.finished) return;\n    if (chunk.created) this.createdAt = chunk.created;",
      "replacement": "    if (true) return;\n    if (chunk.created) this.createdAt = chunk.created;"
    }
  ]
}
